---
description: Logical Tailwind + shadcn via @repo/ui — open ui-design.md for detail
globs: apps/**/src/**/*.tsx,apps/**/src/**/*.css,apps/**/src/app/**/layout.tsx,packages/ui/**/*
alwaysApply: false
---

# UI styling

`lang`/`dir` on **root layout only**. **Logical Tailwind** (`me`/`ms`, `start`/`end`, …).

**shadcn + ReUI** in `@repo/ui` (`packages/ui`) — **never hand-edit** for durable fixes; regen via `pnpm ui:sync` (`sync:shadcn` then `sync:reui`). Durable ReUI overrides live in `packages/ui/scripts/reui/patches/`. **Dimah** (`@repo/ui/components/dimah`) is first-party — edit in place. App behavior in `src/app/**` or `src/components/` (outside package UI).

**Defaults first** — use components with their built-in styles; `className` only when that call site truly needs customization (not to repeat radius, padding, etc. the primitive already provides).

Reuse UI up the tree; hoist to highest relevant scope — [architecture.md § Placement](docs/agents/architecture.md#placement).

**Detail (Read):** [docs/agents/ui-design.md](docs/agents/ui-design.md).
