---
description: Tagged cache — updateTag for same-user UI; open caching.md for detail
globs: apps/**/src/app/action/**/*.ts,apps/**/**/cache-tags.ts,apps/**/src/app/**/lib/get-*.ts
alwaysApply: false
---

# Caching

Next API: read `apps/web` → `node_modules/next/dist/docs/` first — never web-search ([nextjs.md](docs/agents/nextjs.md)). Tags only via segment `cache-tags.ts`.

**Read:** `'use cache'` + `cacheTag` + `cacheLife`. **Write (actor must see fresh UI):** `updateTag` in the **same** action after successful **`auth.api`** — not bare `revalidateTag(tag)`.

Session: never cache or revalidate — [implementation.md § Auth](docs/agents/implementation.md#auth--session).

**Detail (Read):** [docs/agents/caching.md](docs/agents/caching.md).
