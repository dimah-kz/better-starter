# @better-starter/db

PostgreSQL via Drizzle — schema, client, and migrations.

The shared database layer for auth, storage metadata, and anything else that needs durable data. Generate and migrate from here; apps import the client and schema, they don’t own the DB setup.
