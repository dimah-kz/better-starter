# @better-starter/i18n

Shared locale config, message catalogs, and formatting helpers.

Product copy lives here so every app speaks the same language. Next.js wires catalogs through `next-intl`; the package itself stays framework-light.
